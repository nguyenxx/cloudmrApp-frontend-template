import './Setup.scss';
import { Grid, Box } from '@mui/material';

const Setup = () => {
    return (
        <Grid container spacing={2}>
            <Grid item xs={12} md={5}>
                
            </Grid>
        </Grid>
    );
};

export default Setup;
